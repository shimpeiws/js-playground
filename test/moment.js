import moment from 'moment'
import assert from 'assert'

describe('string to moment', function () {
  it('return collect format', () => {
    const timeString = '2016/05/20 13:21:57'

    assert.equal(result, new List())
  })
})
